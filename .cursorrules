# LabelPlusFX 開發規則

## 國際化處理規則

### 新需求文本處理規則

**重要：對於新需求涉及的文本，不需要進行國際化處理，直接使用簡體中文。**

#### 具體規定：

1. **新功能開發**：
   - 所有新功能的用戶界面文本直接使用簡體中文
   - 不需要在語言包文件中添加對應的鍵值對
   - 不需要考慮多語言支持

2. **錯誤信息和提示**：
   - 新功能的錯誤信息、警告、提示等直接使用簡體中文
   - 不需要通過 `I18N` 資源包獲取

3. **菜單和按鈕**：
   - 新增的菜單項、按鈕文本直接使用簡體中文
   - 不需要在 `Lang_*.properties` 文件中定義

4. **對話框和窗口**：
   - 新對話框的標題、內容、按鈕等直接使用簡體中文
   - 不需要國際化處理

#### 示例代碼：

```kotlin
// ✅ 正確：直接使用簡體中文
val dialog = Alert(Alert.AlertType.INFORMATION)
dialog.title = "信息"
dialog.headerText = "操作成功"
dialog.contentText = "文件已保存"

// ❌ 錯誤：使用國際化（新需求不需要）
dialog.title = I18N["dialog.info.title"]
dialog.headerText = I18N["dialog.success.header"]
dialog.contentText = I18N["dialog.file.saved"]
```

#### 注意事項：

- 此規則僅適用於**新需求**，不影響現有功能的國際化支持
- 現有的國際化代碼保持不變，繼續支持多語言
- 如果後續需要為新功能添加國際化支持，可以單獨處理

#### 技術實現：

- 新代碼中避免使用 `I18N["key"]` 的方式獲取文本
- 直接使用中文字符串字面量
- 保持代碼的簡潔性和可讀性

---

*此規則自 2024年12月19日 起生效*

## 編譯與運行規則

### Maven 編譯運行命令

**標準編譯運行命令：**
```bash
mvn clean compile exec:java@run
```

#### 使用說明：

1. **編譯與運行**：
   - 使用上述命令進行完整的清理、編譯和運行
   - 該命令會自動清理 target 目錄、重新編譯所有源碼並運行程序

2. **替代命令**：
   - 僅編譯：`mvn clean compile`
   - 僅運行（需先編譯）：`mvn exec:java@run`

3. **開發流程**：
   - 每次修改代碼後，使用完整命令 `mvn clean compile exec:java@run`
   - 確保所有更改都能正確編譯和運行

#### 注意事項：

- 使用 `exec:java@run` 而不是 `exec:java` 來指定正確的運行配置
- 每次重大修改後建議使用 `clean` 來確保完全重新編譯
- 如遇到類找不到等問題，首先嘗試完整的 clean compile 流程

---

*編譯運行規則自 2025年7月23日 起生效* 